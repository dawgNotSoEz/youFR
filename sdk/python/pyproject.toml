[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-truth-layer"
version = "0.1.0"
description = "Python SDK for the AI Truth Layer verification API"
requires-python = ">=3.10"
dependencies = ["requests>=2.32.0"]
